export interface Iproduct {
    id: string;
    name: string;
    price: number;
    image: string;
    category: string; // إضافة خاصية الفئة
    color: string; // إضافة خاصية اللون
    rating: number; // إضافة خاصية التقييم
}

